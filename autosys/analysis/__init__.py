"""
Phase-1 "Discover & Analyze" migration-assessment logic.

Framework-free (no click/rich/fastapi) so it can be imported by both the
CLI (`autosys analyze`) and the REST API (`/api/v1/assessment/*`) without
either depending on the other.
"""
